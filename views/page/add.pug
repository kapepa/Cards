extends layout

block content
  h1= title
  #isError
    if isError
      div(class="col s6 offset-s3")
        p(class="error")= isError
  form(class="col s6 offset-s3" method="post" action="/add/addition")
    .row
      div(class="input-field col s12")
        input(id="name" name="name" type="text" class="validate" required)
        label(for="email") Name
        span(class="helper-text" data-error="wrong")
      div(class="input-field col s12")
        input(id="price" name="price" type="number" class="validate" required)
        label(for="email") Price
        span(class="helper-text" data-error="wrong")
      div(class="input-field col s12")
        input(id="url" name="url" type="url" class="validate" required)
        label(for="url") Url
        span(class="helper-text" data-error="wrong")
      div(class="input-field col s12")
        input(type="hidden" name="courseID")
        input(type="hidden" name="_csrf" value=csrfToken)
        button(type="submit" class="btn") Addition

